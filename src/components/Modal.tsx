import React from "react";

// sizes
// title
// close 
// content 

type Props = {
    sizes: "small" | "medium" | "large";
    title: string;
    setClose: React.Dispatch<boolean>;
    close: boolean;
    contnet: React.FC;
}

const Modal = ({
    
}) => {

}

export default Modal;
